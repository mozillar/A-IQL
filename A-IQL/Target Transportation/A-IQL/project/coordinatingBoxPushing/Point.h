#ifndef POINT_H
#define POINT_H
struct Point{  //public inherit: Subclasses cannot inherit private members of their parent class, but they can access private members of their parent class through public functions
	double x;
	double y;
};
#endif